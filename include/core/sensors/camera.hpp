#ifndef THYME_ENVIRONMENTS_ROVERS_CAMERA
#define THYME_ENVIRONMENTS_ROVERS_CAMERA

/*

removed for python branch

#include <Eigen/Dense>
#include <utilities/math/linear_algebra.hpp>
#include <numeric>
#include <ranges>
#include <vector>

namespace rovers {

template <typename StackingPolicy>
class Camera {
   public:
    Camera() : fov() {}
    [[nodiscard]] Eigen::MatrixXd scan(const Agent& rover, const std::vector<Agent>& rovers,
                                       std::vector<Entity>& pois) const {
                                        return {};
                                       }
};
}  // namespace thyme::environments::rovers

*/

#endif